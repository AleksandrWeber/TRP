#!/usr/bin/env bash
set -euo pipefail
# Optional Docker scaffold — not enabled in workflows by default.
echo "Docker build scaffold is prepared but disabled."
echo "Enable the workflow step and provide a Dockerfile when ready."
exit 0
