/**
 * Injection token for CampaignRepository.
 * Bind a concrete implementation (e.g. InMemoryCampaignRepository) in the module.
 */
export const CAMPAIGN_REPOSITORY = Symbol('CAMPAIGN_REPOSITORY');
