// Bump this only when the research calculation semantics change
// from the perspective of Knowledge result identity.
export const RESEARCH_ENGINE_VERSION = '1.0.3';

// Bump this only when validation interpretation semantics change.
export const VALIDATION_VERSION = '1.0.2';

// Bump this when the Knowledge payload shape/meaning changes.
export const KNOWLEDGE_SCHEMA_VERSION = 2;
