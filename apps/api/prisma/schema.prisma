generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Sprint 0: minimal schema — business models come in later sprints.
model HealthCheck {
  id        String   @id @default(cuid())
  checkedAt DateTime @default(now())
}
